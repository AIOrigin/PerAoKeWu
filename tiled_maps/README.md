# Tiled MCP project directory
#
# Put your `.tmj` / `.tsj` map files here.
# Cursor MCP server `tiled` (npm: tiled-mcp) uses this folder as TILED_PROJECT_DIR.
#
# Note: Capybara Rush does not use Tiled; this is for separate 2D tilemap work.
