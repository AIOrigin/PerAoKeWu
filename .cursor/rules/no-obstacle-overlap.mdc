---
description: 跑道障碍/机关互斥死线——任何摆放不得重叠
globs: assets/maps/route_levels/**/*.{gd,json}
alwaysApply: true
---

# 障碍物不得重叠（死线）

往跑道注入或移动 **任何** 可交互物时，**禁止与已有障碍/机关重叠**。这是硬性死线，不是偏好。

## 适用范围

- 弹射垫 / 加速垫（`launch_pads`）
- 跳跃、铲跳、紫球、陨石、异能怪、光波门 / 列车门、占位箱等 `obstacles`
- 分叉门、黄门、决策线附属物
- 程序化补障、抉择后恢复段、覆盖扫描

## 必须做的

1. **落点前先查空位**：用路程 + 车道互斥（建议 ≥12–18m），对 **运行时 `obstacles`** 与 **JSON layout** 都查。
2. **门类单独加宽**：`train` / `train_moving` / `meteorite_gate` 旁不放垫（≥18–22m）。
3. **冲突就挪或弃**：宁可少放、往后扫空档，也 **禁止** 叠在 jump / 门 / 怪身上。
4. **注入后可再清一次**：对新建垫跑 overlap purge，重叠的垫直接删掉。

## 禁止

- 为了「看起来有内容」强行塞进已有障碍附近
- 只查 JSON、不查运行时 `obstacles`（程序化补障会漏检）
- 认为「加速段 / 空直道」就一定没障碍而不做检测
