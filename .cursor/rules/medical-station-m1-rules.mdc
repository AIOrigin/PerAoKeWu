---
description: 医疗据点 M1–M4 设计规则与防回归清单
globs: assets/maps/route_levels/**/*medical*
alwaysApply: false
---

# 医疗据点设计规则（以 M1 为模板）

改医疗关 JSON / `runner_60s.gd` 医疗分支时，**必须对齐下列规则**，禁止只改某一关导致其它关回退。

## 硬性死线（全医疗）

1. **障碍/垫不重叠**：遵循 `no-obstacle-overlap`；落点查 **运行时 `obstacles`**，门旁 ≥18–22m。
2. **世界字标白名单**（仅这些可挂 Label3D）：
   - 决策线提示（过线 / 线外减速）
   - `LEFT or RIGHT?`
   - 分叉 `REGULAR` / `SPEEDUP` / `BONUS` + 箭头
   - 垫面 `SPEEDUP` / `LAUNCH`
   - **禁止**自创字标（如火车门 `$ 000`、Nullfeed 字、未约定中文效果说明）
3. **异能抉择每关 1 次**：致死侧随机；错 = 完整度 −50%；对 = 安全。
4. **撞后节奏**：清障空直道 → 冲刺峰值 → 渐慢（地板仍快于常速）→ 回常速；恢复弹射垫固定偏移 **`[32, 54, 76]`m**，前 ~30m 不放垫。
5. **抉择禁放**：弯道、分叉、熔岩坑、侧墙入口/墙段本体、终点附近。
6. **侧墙走廊禁垫**：入口前～墙尾后不放 SPEEDUP/LAUNCH（含抉择恢复垫），否则会「弹飞却看不见垫」。
7. **侧墙后分叉**：下墙后勿用偏左横向默认锁左岔；分叉锁定保持到合路尾 `d_end`。

## Buff 节奏（JSON 对齐 M1）

- **盾晶稀疏**：约 **2** 个，落在空车道；禁止与 jump/orb/train 同车道近距离重叠。
- **加速靴密集**：开局可有短链；中段可有错车道对（如 118/128）；末段可加密。
- **加速垫约 3**：开/中/末直道；避开 train/meteorite/jump（≥16–18m）。
- 勿把「看起来空」当无障；改垫/靴后必须再扫冲突。

## 分关落点（异能抉择 base）

| 关 | 建议 base | 注意 |
|----|-----------|------|
| M1 | ~178 | Emergency |
| M2 | ~410 | 平台坑之后（勿落在 main_block 内） |
| M3 | ~640 | **Y 岔后中后半直道**；严禁侧墙/下墙段（300–372+缓冲） |
| M4 | ~172 | Emergency；恢复走廊保持空 |

Relay **M3** 与 Emergency **共用**撞后清障 + 恢复垫注入（`_is_medical_location()`，不只 `_is_emergency_run`）。

## 改动检查清单

- [ ] M2/M3/M4 是否仍只有约 2 盾、靴密、3 垫且无垫障重叠？
- [ ] 抉择点是否避开侧墙/弯道/分叉？
- [ ] 有无新增未约定的世界字标？
- [ ] 撞后恢复是否仍走 `[32,54,76]` 且不贴砸点？
